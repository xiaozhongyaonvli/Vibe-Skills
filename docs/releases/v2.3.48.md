# VCO Release v2.3.48

- Date: 2026-03-23
- Commit(base): 3cd3ae2

## Highlights

- Normalized legacy `benchmark_autonomous` runtime input to `interactive_governed`, making `interactive_governed` the only effective governed runtime mode while preserving silent compatibility for older callers.
- Updated runtime entry, input freezing, cleanup policy, and governed contract metadata so the legacy benchmark mode no longer routes through unattended execution and now reports `interactive_governed` as the effective mode.
- Reworked runtime bridge and proof gates to verify the compatibility downgrade explicitly, including the governed runtime contract gate and benchmark proof gate.
- Hardened `scripts/verify/vibe-adaptive-routing-readiness-gate.ps1` so a single telemetry file no longer crashes the release train through a scalar `.Count` assumption.

## Validation Notes

- Runtime compatibility downgrade:
  - `python3 -m unittest tests.runtime_neutral.test_governed_runtime_bridge -v`
  - `pwsh -NoProfile -ExecutionPolicy Bypass -File ./scripts/verify/vibe-governed-runtime-contract-gate.ps1`
  - `pwsh -NoProfile -ExecutionPolicy Bypass -File ./scripts/verify/vibe-benchmark-autonomous-proof-gate.ps1`
- Release-train robustness:
  - `pwsh -NoProfile -ExecutionPolicy Bypass -File ./scripts/verify/vibe-adaptive-routing-readiness-gate.ps1`
  - `pwsh -NoProfile -ExecutionPolicy Bypass -File ./scripts/governance/release-cut.ps1 -Version 2.3.48 -Updated 2026-03-23 -RunGates`
- Working tree hygiene:
  - `git diff --check`

## Migration Notes

- Operators and callers may continue sending `benchmark_autonomous`, but runtime truth now records and executes it as `interactive_governed`.
- Any automation that previously assumed `benchmark_autonomous` implied unattended execution should be updated to treat it as a compatibility alias only.
- Release verification on repositories with exactly one adaptive-routing telemetry file now remains green instead of crashing in the readiness gate.

# Archived Status

This directory no longer keeps a tracked leaf set by default.

Historical status leaves were removed from the GitHub-visible docs surface once they stopped carrying active consumers.

## Rule

- Keep this directory only as an archive contract marker.
- Recover historical status leaves from git history unless a live page explicitly reintroduces one as a tracked dependency.

# 2026-03-23 Release v2.3.48 Requirement

- Topic: cut a new governed release for the benchmark-mode compatibility downgrade shipped on `3cd3ae2`.
- Mode: interactive_governed
- Goal: publish the current governed runtime state as `v2.3.48` with release surfaces, notes, and tags aligned to the shipped change.

## Deliverable

A governed release cut that:

1. bumps the canonical release version from `2.3.47` to `2.3.48`
2. records `2026-03-23` as the updated date for the release surface
3. creates a release note describing the `benchmark_autonomous` to `interactive_governed` compatibility downgrade
4. updates release navigation surfaces so `v2.3.48` becomes the current visible release
5. verifies the release cut before commit, push, and tag creation

## Constraints

- Perform release work from a clean worktree aligned to the latest remote `main`
- Do not modify or revert unrelated changes in the user's dirty main worktree
- Use the repository's canonical release script for version/governance updates
- Keep the release scoped to the already-shipped runtime compatibility fix

## Acceptance Criteria

- `config/version-governance.json` reports release version `2.3.48` and updated date `2026-03-23`
- maintenance markers in `SKILL.md` and governed mirrors report `2.3.48`
- `references/changelog.md` contains a top entry for `v2.3.48`
- `docs/releases/v2.3.48.md` exists and accurately summarizes the shipped behavior change
- `docs/releases/README.md` shows `v2.3.48` as the current release surface
- release gates invoked for the cut pass successfully
- a clean git commit and `v2.3.48` tag are pushed to GitHub

## Non-Goals

- Expanding the shipped scope beyond the benchmark compatibility downgrade
- Reworking older historical release notes
- Cleaning up unrelated local branches or worktrees

## Inferred Assumptions

- This release should be a patch bump because it ships a compatibility and default-behavior correction rather than a breaking contract change.
- The previously pushed commit `3cd3ae2` is the correct release base for `v2.3.48`.
- Release truth should explicitly describe that legacy `benchmark_autonomous` input is now normalized to `interactive_governed`.

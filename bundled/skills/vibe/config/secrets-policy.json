{
  "schema_version": 1,
  "updated": "2026-03-06",
  "rules": {
    "allow_plaintext_in_repo": false,
    "require_env_or_vault": true,
    "require_gitignore_for_local_env_files": true,
    "allow_runtime_only_handles": true,
    "default_rotation_days": 30,
    "forbid_checking_tokens_into_docs_examples": true
  },
  "allowed_secret_refs": [
    {
      "name": "ACTIVEPIECES_MCP_TOKEN",
      "scope": "project",
      "storage": [
        "environment",
        "vault"
      ],
      "rotation_days": 30,
      "notes": "Bearer token for project-scoped Activepieces MCP endpoint."
    },
    {
      "name": "COMPOSIO_API_KEY",
      "scope": "user",
      "storage": [
        "environment",
        "vault"
      ],
      "rotation_days": 30,
      "notes": "API key used to create and manage Composio sessions."
    },
    {
      "name": "COMPOSIO_SESSION_MCP_URL",
      "scope": "session",
      "storage": [
        "runtime"
      ],
      "rotation_days": 1,
      "notes": "Ephemeral session MCP URL. Do not persist to repo or long-lived config."
    }
  ]
}

# 2026-03-23 Release v2.3.48 Plan

## Goal

Publish the already-shipped benchmark compatibility downgrade as governed release `v2.3.48`.

## Grade

- Internal grade: M

## Work Batches

### Batch 1: Governance freeze
- Create a release requirement document
- Create a release execution plan

### Batch 2: Clean release base
- Use the clean remote-aligned worktree at commit `3cd3ae2`
- Confirm the worktree starts with no local changes

### Batch 3: Release cut
- Run `scripts/governance/release-cut.ps1` in preview mode for `2.3.48`
- Apply the release cut with `-RunGates`
- Let the canonical script update version governance, maintenance markers, changelog, release ledger, and the initial release note file

### Batch 4: Release surface completion
- Replace the generated `docs/releases/v2.3.48.md` TODO template with accurate highlights, validation notes, and migration notes
- Update `docs/releases/README.md` so `v2.3.48` is the current release surface and appears in recent governed releases

### Batch 5: Verification
- Run `git diff --check`
- Run `git status --short` to confirm only intended release-surface files changed
- Review the diff for version, release note, changelog, and mirror correctness

### Batch 6: Publish
- Commit the release-surface changes with a release commit message
- Push the commit to `origin/main`
- Create and push git tag `v2.3.48`

## Rollback Rules

- If release-cut preview shows unexpected surfaces, stop before apply and inspect the governance contract first.
- If any release gate fails, do not commit or tag until the failure is understood and resolved.
- If push or tag publication fails, keep the local release commit intact and report the exact remote state instead of guessing.
